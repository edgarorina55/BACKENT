Pandas

.python data analysis
.open source library for data manipulation and analysis 
    .efficiently manages large datasets - offers tools for data cleaning, transformation, and analysis
    .integrates with NumPy, Matplotlib, and Scikit-learn
    .revolves around 1D and 2D data structures
        .series 
        .dataframes 

Uses:
    .reading and writing data from various file formats like CSV, Excel, SQL DBs
    .cleaning and preparing data - handling missing values, filtering, removing duplicates 
    .merging, joining, and reshaping datasets
    .performing statistical analysis and descriptive statistics 
    .visualizing data quickly 




DataFrame 
    .2D 
    .size-mutable 
    .potentially heterogeneous tabular data structure with labeled axes 








CSV
.comma separated values files 
    .plain text files where each row reps a record and columns are separated by commas 
.allow storage of tabular data 
.Pandas retrieves data in the form of data frame 

    .reading csv files into Pandas DataFrame  
        .read_csv() function 

            pd.read_csv(filepath_or_buffer, sep=',' ,header='infer',
                index_col=None, usecols=None, engine=None,
                skiprows=None, nrows=None)


