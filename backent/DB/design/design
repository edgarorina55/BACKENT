Designing DB

.its creating a detailed data model of a DB
.involves defining structure, storage, and retrieval mechanisms of the data 
    .to ensure it meets the needs of the users and applications that will interact with it 
.well-designed DB allows for efficient data mgt, retrieval, and storage, ensuring data integrity and security 


Need 
.efficiency - efficient storage, fast retrieval, enhanced overall performance 
.scalability - easier to scale as the data grows/ change in application requirements 
.data integrity - data remains accurate and consistent throughout its lifecycle 
.maintenance - simplified maintenance tasks like updates, backups, thus reducing risk of errors and downtime 


Process
.Requirements and analysis 
    .gather requirements - from stakeholders 
        .know what data, how it will be used, and any constraints 
    .identifying entities and relationships 
        .are objects/concepts to be stored & their interactions e.g customer placing an order 

.Conceptual design 
    .entity-relationship diagrams (ERD)
        .visual representations of entities and relationships within the DB
        .help organize and clarify the structure of the DB in a clear manner
    .identifying attributes 
        .are specific pieces of information that need to be stored about each entity e.g customer name 
    .defining relationships 
        .specify how entities are related to each other e.g one-to-one, one-to-many
        .specify any constraints 

Logical design 
    .converting ERD to tables 
        .ERD is translated into a set of tables 
        .each entity becomes a table, and each attribute becomes a column within that table 
    .normalization 
        .its organizing data to minimize redundancy and improve data integrity 
        .normal forms:
            .1NF: first normal form 
                .ensures that each column contains atomic (indivisible) values and that each column contains only one type of data 
            .2NF: second normal form 
                .build on 1NF by ensuring all non-key attributes are fully functionally dependent on the primary key
            .3NF: third normal form 
                .ensures all attributes are functionally dependent only on the primary key, removing any transitive dependencies 
            .BCNF: boyce-codd normal form 
                .stricter version of the 3NF where every determinant is a candidate key, ensures higher normalization
    .integrity constraints 
        .ensure data accuracy and consistency 
            e.g primary and foreign keys, unique constraints, and check constraints 


.Physical Design 
    .choosing storage engines 
        .different engines different features such as transaction support and locking mechanisms e.g InnoDB for mysql
    .indexing 
        .proper indexing is crucial for performance optimization - provide quick access paths to data
    .partitioning 
        .divides a large table into smaller, more manageable pieces, which can improve performance and manageability
    .optimizing performance
        .involves fine-tuning the DB design, queries, and hardware to ensure efficient operation


.Testing and evaluation 
    .perform testing 
        .conduct tests to ensure DB functions correctly, including unit tests, integration tests, and performance tests 
    .evaluate performance 
        .analyze the DB performance and make necessary adjustments to optimize speed and efficiency 


.Deployment 
    .deploy DB
        .move DB to a production environment where it will be accessible to end-users 
    .train users
        .provide training and documentation for users to effectively interact with the DB


.Maintenance and monitoring 
    .monitor performance 
        .continuously monitor performance to identify and resolve issues promptly
    .perform maintenance
        .regularly update and maintain the DB to ensure its continued performance and security, including backups and updates 

        





.Security and integrity
    .data integrity constraints
        .implement constraints like primary key to maintain data integrity 
    .user access control 
        .control who can access and modify data by setting up user roles and permissions
    .encryption and backup 
        .encrypt sensitive data to protect it from unauthorized access and regularly back up your DB to prevent data loss 


.Best practices 
    .avoiding redundancy - ensure that each piece of data is stored only once to avoid inconsistencies and wasted storage 
    .ensuring scalability - design DB with future growth in mind 
    .regulary review and update the design - to address changing requirements and improve performance 


