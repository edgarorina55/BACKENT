Models 

.model 
    .Python class that represents a DB table 
    .helps manage data easily using Python instead of writing MySQL
    .ORM easens connecting Python code with DB
    .helps create, update, and delete data easily 
    .supports data checks and table relationships using fields like Foreign Key 




Creating a Model
.need project and app before defining any models
    .models are then created after creating the app

        from django.db import models

        class GeeksModel(models.Model):
            title = models.CharField(max_length=200)
            description = models.TextField()
            image = models.ImageField(upload_to='images/')

            def __str__(self):
            return self.title

>>above defines a django model called GeeksModel with three fields:
    .title - character field with 200-character limit 
    .description - text field for longer input 
    .image - imageField used to store the path or reference to an uploaded image file 
    .__str__method - defined to return the title of the instance of the model when the model is printed 


    .the model is converted into a DB table through the migration system, which tracks changes and applies them to 
    the DB. 
    .django maps the fields defined in the models into table fields of the DB 




Applying migrations
>>
.ORM simplifies DB interactions by mapping Python objects to DB tables 
    .migrations are key in this: they enable seamless mgt of changes to the DB schema over time 

    Migrations:
        .files that store instructions about how to modify the DB schema 
        .they help ensure that the DB is in sync with the models defined in the django project 
        .whenever changes are made to models, such as CRUD fields, django uses migrations to apply these changes to the DB
            .makemigrations - creates migration files(python code) based on changes made to the models 
            .migrate - applies the generated migration files by executing the corresponding SQL commands on the DB 

    step 1: makemigrations command 
        .generates migration files that describe changes to the DB schema in Python code 
            .creates new tables when new models are added 
            .adds new fields to existing models
            .alters existing fields or modifies the table structure 
                >>when Python manage.py makemigrations is run, django detects changes in the models and generates migration files 
                    .migration files are created inside the migrations/directory of the app
                    .each file is automatically numbered to represent incremental changes 
                    .these files describe changes to the DB schema in Python code, ready to be applied with migrate 

    step 2: migrate command 
        .after creating migration files using makemigrations, the migrate command applies these changes to the DB
            .reads all migration files in the project
            .performs necessary updates to the DB schema
            .creates or modifies tables and fields as defined in the migration files 
                >>python manage.py migrations command applies the changes in the correct order by reading the migration files generated by makemigrations. It handles:
                    .creating tables
                    .modifying columns
                    .adding indexes
                    .performing other DB-related operations  














Registering models in django admin 
.open admin.py in app directory
.register the model 
        from django.contrib import admin 
  
        # Register your models here. 
        from .models import GeeksModel 
        
        admin.site.register(GeeksModel)

        .code doesn't produce any output; only registers the GeeksModel with the admin site so that it can be managed via the django admin interface 
        .the admin panel can now CRUD model instances 


    
Model Field Types 
.fields define the structure of DB tables 
    .each field specifies:
        .type of data it stores 
        .constraints and validations on the data 
            CharField: Stores short text (like names or titles)
            TextField: Stores large amounts of text (like descriptions)
            IntegerField: Stores integer/whole number values
            FloatField: decimals 
            BooleanField: Stores True/False values
            DateTimeField(auto_now_add=True) - created timestamp
            DateTimeField(auto_now=True) - updated timestamp
            ForeignKey(Model, on_delete=models.CASCADE) - relationships 
            EmailField - validated email 
            JSONField() - structured data (PostgreSQL native, MySQL supported via Django)
            ImageField/FileField: Stores images or files




Relationship fields 
.allow easy querying of related objects using django ORM and automatically handle DB constraints 
.used when models are related to each other 
    ForeignKey: Many-to-one relationship.
    OneToOneField: One-to-one relationship.
    ManyToManyField: Many-to-many relationship.




Field options 
.every field in models can have options(attributes) to control its behavior 
.make django models highly flexible and ensure data integrity in the DB and forms 
.they allow:
    Set constraints (null, blank, unique)
    Provide default values (default)
    Customize display names (verbose_name)
    Add help text for forms (help_text)
    Apply validators to enforce custom rules




CRUD operations on models  
.ORM allows interaction with the DB using Python code instead of raw SQL
    .create - add new records to the DB and save them 
    .retrieve - query and fetch existing records easily
    .update - modify existing records and save the changes 
    .delete - remove records safely from the DB 
